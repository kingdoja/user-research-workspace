# Server-only OpenAI credential. Never prefix this with NEXT_PUBLIC_.
OPENAI_API_KEY=

# Optional model overrides. Defaults to gpt-5.6-terra for both stages.
OPENAI_PLAN_MODEL=gpt-5.6-terra
OPENAI_RESEARCH_MODEL=gpt-5.6-terra

# Optional OpenAI-compatible endpoint override for local integration testing.
# OPENAI_BASE_URL=http://127.0.0.1:4010/v1

# Local embedded Postgres directory.
PGLITE_DATA_DIR=.data/atypica
